import React from 'react'
import Layout from '../core/Layout'

const AddProduct = () => {
    return (
        <Layout title="Add Product" description={`Admin : qwertyu `} className="container">
        ..............
        </Layout>
    )
}

export default AddProduct
